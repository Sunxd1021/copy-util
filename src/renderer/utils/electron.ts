// const empty = () => {}

// const electron = new Proxy(window, {
//   get: (target, key: string) => {
//     const _ele = target.electron;
//     return _ele ? _ele[key] : empty;
//   }
// });

// export default electron;
